{% extends '::base2.html.twig' %}

{% block body -%}
    <h1>Plat Du Jour</h1>

    <table class="table table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ entity.prix }}</td>
            </tr>
            <tr>
                <th>Desciption</th>
                <td>{{ entity.desciption }}</td>
            </tr>
            <tr>
                <th>Disponible</th>
                <td>{{ entity.disponible }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('restaurant_platsdujour') }}">
            Retour
        </a>
    </li>
    <li>
        <a href="{{ path('restaurant_platsdujour_edit', { 'id': entity.id }) }}">
           <span class="icon icon-edit icon-3x"></span>
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
