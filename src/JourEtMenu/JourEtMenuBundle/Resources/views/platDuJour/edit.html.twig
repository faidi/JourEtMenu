{% extends '::base2.html.twig' %}

{% block body -%}
    <h1>platDuJour edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('restaurant_platsdujour') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
