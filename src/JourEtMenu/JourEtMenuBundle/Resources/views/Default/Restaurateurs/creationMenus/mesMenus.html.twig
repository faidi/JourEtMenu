{% extends '::base2.html.twig' %}

{% block body -%}
<div class=" col-md-8">
    <h1>Mes menus </h1>
    <hr>
	
         <a class="btn btn-success " href="{{ path('BoardMenuscreation') }}">Publier un nouveau menu</a>
    	 <hr>
    <table class="table table-hover">
        <thead>
            <tr>
                 
                <th>Nom</th>
                <th>Prix</th>
                <th>Desciption</th>
                 
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                
                <td>{{ entity.nom }}</td>
                <td>{{ entity.prix }}</td>
                <td>{{ entity.description }}</td>
                 
                <td>
                <td><a class="btn btn-default"
					href="#"><span
						class="icon icon-look">Voir</a> <a
					class="btn btn-primary" href="#"><span
						class="icon icon-edit"></span></a></td>
			 </a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

         
     </div>
    {% endblock %}
